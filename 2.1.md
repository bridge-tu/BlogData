#递归函数

##斐波那契数列
```cpp
unsigned long fib(unsigned long n)
{if(n<=1)
  return 1;

return fib(n-1)+fib(n-2);//再调用回自己函数
}
```

#练习
##有一段楼梯，有n阶台阶，可以一次爬一层，一次爬两层，那共有多少种爬法
```cpp
#include <iostream>
using namespace std;
int calc(int n)
{
if(n<=0) return 0;
if(n=1) return 1;
 if(n=2) return 2;
 return calc(n-1)+calc(n-2);
}
int main(void)
{
    for(int i=1;i<40;i++){
        cout<<"("<<i<<")"<<calc(i)<<end1;
    }
    return 0;
}
//有缺陷：50时会超过int所承受的范围，需要用动态规划
```

##动态规划改进代码
```cpp
int calc(int n){
int dp[n+1]={0,1,2};
for(int i=3;i<=n;i++){
   dp[i]=dp[i-1]+dp[i-2]; 

}
}
```













